# __TARS_PROJECT_NAME__

A Node CLI app, scaffolded by [TARS](https://github.com). No dependencies:
uses Node's built-in test runner (`node --test`) and `node --check` as a
syntax-validation "build" step, so it runs immediately with no `npm
install`.

## Usage

```sh
node src/index.js World
```

## Test

```sh
npm test
```
